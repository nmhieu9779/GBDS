import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  boxShadow: {
    elevation: 2,
    shadowColor: "#000000",
    shadowOpacity: 0.5,
    shadowRadius: 1,
    shadowOffset: {
      height: 0.5,
      width: 0
    }
  }
})

export default styles
