import { moderateScale } from "@src/utilities/scale"

const stylesheets = {
  boxShadow: {
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: moderateScale(3)
    },
    shadowOpacity: 0.27,
    shadowRadius: 4.65,
    elevation: 6
  }
}

export default stylesheets
